
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Haskell / zinc build artifacts
dist-newstyle/
.zinc/
*.hi
*.o
result
result-*

# direnv / Nix dev-env cache
.direnv/
.envrc

# machine-specific self-host lock (regenerated by `zinc update`; commit once the
# colour mirror is hosted at a stable URL — zinc-576.11)
zinc.lock
