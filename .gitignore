
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Haskell / zinc build artifacts
dist-newstyle/
.zinc/
*.hi
*.o
# per-developer local dependency overrides (zinc-g1b); never committed
zinc.local.toml
result
result-*

# direnv / Nix dev-env cache
.direnv/
.envrc

# local docs preview
docs/_site/
docs/.jekyll-cache/
docs/Gemfile.lock
docs/Gemfile
